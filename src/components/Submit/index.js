import React from "react";

// Submit component will send the users answer to our database via onClick.
function Submit() {
  return (
    <button>onClick This will submit the users onChange input value</button>
  );
}

export default Submit;
